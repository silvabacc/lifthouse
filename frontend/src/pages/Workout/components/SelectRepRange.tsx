import { Select } from "antd";
import styled from "styled-components";

const SelectRepRange = styled(Select)`
  width: 100%;

  .ant-select-arrow {
    color: #000;
  }
`;

export default SelectRepRange;
