import { Button } from "antd";
import styled from "styled-components";

const WorkoutButtonStyles = styled(Button)`
  margin-top: 12px;
  margin-bottom: 12px;
  width: 50%;
`;

export default WorkoutButtonStyles;
